import React from 'react';
import { Typography, Button } from '@mui/material';
import '../authStyles.css';


const RegisterPageHeader = ({ setType }) => {
    return (
        <>
            <Typography variant="h4">Create Account</Typography>
        </>
    );
};

export default RegisterPageHeader;